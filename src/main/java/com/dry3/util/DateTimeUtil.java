package com.dry3.util;import org.apache.commons.lang3.StringUtils;import org.joda.time.DateTime;import org.joda.time.format.DateTimeFormat;import org.joda.time.format.DateTimeFormatter;import java.util.Date;/** * Created by dry3 */public class DateTimeUtil {    //joda-time    private static String STANDARD_FORMAT = "yyyy-MM-dd HH:mm:ss";    //Str -> Date    public static Date strToDate(String dateTimeStr, String formatStr) {        DateTimeFormatter dateTimeFormat = DateTimeFormat.forPattern(formatStr);        DateTime dateTime = dateTimeFormat.parseDateTime(dateTimeStr);        return dateTime.toDate();    }    public static Date strToDate(String dateTimeStr) {        return strToDate(dateTimeStr, STANDARD_FORMAT);    }    //Date -> Str    public static String dateToStr(Date strTimeDate, String formatStr) {        if (strTimeDate == null) {            return StringUtils.EMPTY;        }        DateTime dateTime = new DateTime(strTimeDate);        return dateTime.toString(formatStr);    }    public static String dateToStr(Date strTimeDate) {        return dateToStr(strTimeDate, STANDARD_FORMAT);    }/* 测试utils public static void main(String[] args) {        System.out.println(DateTimeUtil.dateToStr(new Date(),"yyyy-MM-dd HH:mm:ss"));        System.out.println(DateTimeUtil.strToDate("2010-05-14 21:41:12", "yyyy-mm-dd HH:mm:ss"));    } */}